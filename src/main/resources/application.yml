spring:
  application:
    name: spring-boot-besu-tip-jar

web3:
  rpc-url: http://localhost:8545
  # Besu dev-mode pre-funded account 0xfe3b557e8fb62b89f4916b721be55ceb828dbd73
  # (publicly known key, local dev only)
  private-key: "0x8f2a55949038a9610f50fb23b5883af3b4ecb3c3bb792cbcefbd1542c692be63"
  # The TipJar all /api/tipjar endpoints talk to. This default is the address the
  # FIRST contract deployed by the account above always gets on a fresh chain
  # (derived from sender + nonce 0). After other transactions, POST /api/contract/deploy
  # and paste the returned address here, then restart.
  contract-address: "0x42699a7612a82f1d9c36148af9c77354759b210b"
  gas-price: 0
  gas-limit: 4000000
